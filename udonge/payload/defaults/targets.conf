# udonge — target configuration
#
# mode = whitelist  -> ONLY the apps listed below are cloaked
# mode = blacklist  -> ALL apps are cloaked EXCEPT those listed below
#
# One package name per line. Blank lines and lines starting with '#' are ignored.
# The built-in target set is bounded to avoid waking the root companion for
# every app process.

mode = whitelist

# --- Anti-detection scanner (must be listed so module is injected into it) ---
com.eltavine.duckdetector

# --- NSPK payment systems ---
# stealth mode: inject for FORCE_DENYLIST_UNMOUNT (hides su), DLCLOSE before libgp.so loads
stealth:ru.nspk.mirpay
ru.nspk.sbpay

# --- Russian banks ---
ru.sberbankmobile
com.idamob.tinkoff.android
ru.vtb24.mobilebanking.android
ru.alfabank.mobile.android
ru.gazprombank.android.mobilebank.app
ru.raiffeisennews
ru.rosbank.android
ru.mkb.mobile
ru.rshb.dbo
ru.letobank.Prometheus
com.openbank
ru.sovcombank.halva
com.sovcombank.club

# --- Payment / wallet services ---
ru.yoo.money
com.yandex.bank
ru.ozon.fintech.finance
com.qiwi.wallet

# --- Game (anti-cheat root detection) ---
com.axlebolt.standoff2
