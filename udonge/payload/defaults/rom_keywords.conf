# ROM keyword filter — one keyword per line (minimum 3 characters).
# File paths, property names, and property values containing these keywords
# will be hidden from detection apps. Restart required after changes.
#
# Examples:
#   lineage
#   protonaosp
#   crdroid
