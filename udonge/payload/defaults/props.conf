# udonge — faked system properties (per-target-process, via libc plt hook).
# key = value. Blank lines and '#' comments ignored.
#
# Boot state props are now hardcoded in the native hook and also set system-wide
# via system.prop — you don't need to list them here unless you want to override
# the hardcoded values for some reason.
#
# Add custom per-app prop overrides below if needed.
