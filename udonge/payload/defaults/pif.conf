# Google Play certification (Play Protect + MEETS_BASIC_INTEGRITY).
# udonge overwrites these android.os.build fields inside the gms/droidguard
# processes (via JNI) with a certified Pixel profile. Delete this file to
# disable the certification spoof.
#
# key = value, where key is the android.os.Build field name
# (SECURITY_PATCH and DEVICE_INITIAL_SDK_INT live on android.os.Build.VERSION).
#
# Google rotates/bans public fingerprints — refresh this from a current source
# such as raw.githubusercontent.com/vladrevers/pifsync/main/pif.json if
# certification stops working. NOTE: this passes BASIC / certification only;
# hardware MEETS_DEVICE_INTEGRITY needs a keybox (TrickyStore), not a fingerprint.

FINGERPRINT=google/tegu_beta/tegu:CANARY/ZP11.260618.005/15760424:user/release-keys
BRAND=google
DEVICE=tegu
MANUFACTURER=Google
MODEL=Pixel 9a
PRODUCT=tegu_beta
ID=ZP11.260618.005
TAGS=release-keys
TYPE=user
SECURITY_PATCH=2026-07-05
DEVICE_INITIAL_SDK_INT=32
RELEASE=15
INCREMENTAL=15760424
